import { useEffect, useState, type FormEvent } from 'react';
import { useTranslation } from 'react-i18next';
import { z } from 'zod';
import { SelectField, TextArea, TextField } from '../../components/forms';
import { createEncryptedRepository, type ContinuityDatabase } from '../../data/repositories/encryptedRepository';

const base = { id: z.string(), schemaVersion: z.number(), createdAt: z.string(), updatedAt: z.string() };
export const outstandingPurchasesSchema = z.object({ ...base, merchantProvider: z.string(), type: z.string(), amount: z.string(), datePaid: z.string(), expectedRefundService: z.string(), orderReference: z.string(), paymentSource: z.string(), contact: z.string(), supportingDocument: z.string(), supportingDocumentLocation: z.string(), primaryContact: z.string(), preparerNotes: z.string(), notes: z.string(), printPolicy: z.enum(['full', 'last4', 'hidden']), includeInPrint: z.boolean(), includeInReadableExport: z.boolean() });
type RecordType = z.infer<typeof outstandingPurchasesSchema>;
type FormState = Omit<RecordType, 'id' | 'schemaVersion' | 'createdAt' | 'updatedAt'>;
const empty: FormState = { merchantProvider: '', type: '', amount: '', datePaid: '', expectedRefundService: '', orderReference: '', paymentSource: '', contact: '', supportingDocument: '', supportingDocumentLocation: '', primaryContact: '', preparerNotes: '', notes: '', printPolicy: 'hidden', includeInPrint: true, includeInReadableExport: true };

export function OutstandingPurchasesRefunds({ database, dek }: { database: ContinuityDatabase | null; dek: CryptoKey }) {
  const { t } = useTranslation('v2', { keyPrefix: 'outstanding_purchases_deposits_refunds' });
  const [records, setRecords] = useState<RecordType[]>([]); const [form, setForm] = useState<FormState>(empty); const [saved, setSaved] = useState(false);
  const load = () => { if (!database) return; void createEncryptedRepository(database, dek, 'OutstandingPurchaseRecord', outstandingPurchasesSchema).list().then(setRecords); };
  useEffect(load, [database, dek]);
  const save = async (event: FormEvent) => { event.preventDefault(); if (!database || !form.merchantProvider.trim() || !form.type.trim()) return; const now = new Date().toISOString(); await createEncryptedRepository(database, dek, 'OutstandingPurchaseRecord', outstandingPurchasesSchema).put({ id: crypto.randomUUID(), schemaVersion: 1, createdAt: now, updatedAt: now, ...form, merchantProvider: form.merchantProvider.trim(), type: form.type.trim() }); setForm(empty); setSaved(true); load(); };
  return <section className="section-page v2-feature"><p className="eyebrow">{t('title')}</p><h2>{t('title')}</h2><p>{t('intro')}</p><p className="security-clarification">{t('caution')}</p><div className="section-card"><h3>{t('summary')}</h3>{records.length === 0 ? <p>{t('empty')}</p> : <ul>{records.map((record) => <li key={record.id}>{record.merchantProvider} — {record.type} — {record.amount}</li>)}</ul>}</div><div className="section-card"><h3>{t('add')}</h3><form onSubmit={(event) => void save(event)}><div className="form-grid"><TextField label={t('merchantProvider')} value={form.merchantProvider} onChange={(event) => setForm({ ...form, merchantProvider: event.target.value })} required /><TextField label={t('type')} value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value })} required /><TextField label={t('amount')} value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} /><TextField label={t('datePaid')} type="date" value={form.datePaid} onChange={(event) => setForm({ ...form, datePaid: event.target.value })} /><TextField label={t('expectedRefundService')} value={form.expectedRefundService} onChange={(event) => setForm({ ...form, expectedRefundService: event.target.value })} /><TextField label={t('orderReference')} value={form.orderReference} onChange={(event) => setForm({ ...form, orderReference: event.target.value })} /><TextField label={t('paymentSource')} value={form.paymentSource} onChange={(event) => setForm({ ...form, paymentSource: event.target.value })} /><TextField label={t('contact')} value={form.contact} onChange={(event) => setForm({ ...form, contact: event.target.value })} /></div><TextArea label={t('supportingDocument')} value={form.supportingDocument} onChange={(event) => setForm({ ...form, supportingDocument: event.target.value })} /><TextArea label={t('supportingDocumentLocation')} value={form.supportingDocumentLocation} onChange={(event) => setForm({ ...form, supportingDocumentLocation: event.target.value })} /><TextField label={t('primaryContact')} value={form.primaryContact} onChange={(event) => setForm({ ...form, primaryContact: event.target.value })} /><TextArea label={t('preparerNotes')} value={form.preparerNotes} onChange={(event) => setForm({ ...form, preparerNotes: event.target.value })} /><TextArea label={t('notes')} value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /><SelectField label={t('printPolicy')} value={form.printPolicy} onChange={(event) => setForm({ ...form, printPolicy: event.target.value as RecordType['printPolicy'] })} options={(['full', 'last4', 'hidden'] as const).map((value) => ({ value, label: t(value) }))} /><label><input type="checkbox" checked={form.includeInPrint} onChange={(event) => setForm({ ...form, includeInPrint: event.target.checked })} /> {t('includeInPrint')}</label><label><input type="checkbox" checked={form.includeInReadableExport} onChange={(event) => setForm({ ...form, includeInReadableExport: event.target.checked })} /> {t('includeInReadableExport')}</label><button className="button button-primary">{t('save')}</button>{saved && <p role="status">{t('saved')}</p>}</form></div><div className="section-card paper-actions"><h3>{t('paperActions')}</h3><p>☐ {t('contacted')}  ☐ {t('refundReceived')}  ☐ {t('serviceCompleted')}  ☐ {t('canceled')}</p><p>{t('actualAmount')}: __________  {t('date')}: ____ / ____ / ______  {t('reference')}: ____________________</p><div className="handwriting-lines" aria-hidden="true" /></div></section>;
}
