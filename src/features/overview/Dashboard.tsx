import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { householdSchema, type Household } from '../household/HouseholdSetup';
import { createEncryptedRepository, type ContinuityDatabase } from '../../data/repositories/encryptedRepository';

export function Dashboard({ database, dek }: { database: ContinuityDatabase | null; dek: CryptoKey }) { const { t } = useTranslation(); const total = 37; const [household, setHousehold] = useState<Household | null>(null); useEffect(() => { if (!database) return; void createEncryptedRepository(database, dek, 'Household', householdSchema).list().then((records) => setHousehold(records[0] ?? null)); }, [database, dek]); const reviewed = household?.reviewedSections.length ?? 0; return <section className="dashboard"><p className="eyebrow">{t('appName')}</p><h2>{t('dashboardTitle')}</h2><p>{t('dashboardBody')}</p><progress max={total} value={reviewed} aria-label={t('sectionsReviewed', { count: reviewed, total })} /><p>{t('sectionsReviewed', { count: reviewed, total })}</p><div className="dashboard-cards"><article><h3>{t('continueCard')}</h3><p>{t('protectedBody')}</p></article><article><h3>{t('gapsCard')}</h3><p>{t('noGaps')}</p></article><article><h3>{t('printingCard')}</h3><p>{t('protectedBody')}</p></article><article><h3>{t('lastSaveCard')}</h3><p>{t('savedLocally')}</p></article><article><h3>{t('lastBackupCard')}</h3><p>{t('protectedBody')}</p></article></div></section>; }
