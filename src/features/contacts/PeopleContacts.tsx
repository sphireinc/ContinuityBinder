import { useEffect, useState } from 'react';
import * as React from 'react';
import { useTranslation } from 'react-i18next';
import { TextArea, TextField, SelectField } from '../../components/forms';
import { createEncryptedRepository, type ContinuityDatabase } from '../../data/repositories/encryptedRepository';
import { addressSchema, personSchema } from '../household/HouseholdSetup';
import { z } from 'zod';

const base = { id: z.string(), schemaVersion: z.number(), createdAt: z.string(), updatedAt: z.string() };
export const contactSchema = z.object({ ...base, displayName: z.string().min(1), role: z.enum(['attorney', 'cpa', 'financialAdviser', 'insuranceAgent', 'funeralHome', 'clergy', 'physician', 'veterinarian', 'school', 'employerHr', 'businessPartner', 'propertyService', 'friendFamily', 'other']), phone: z.string().optional(), email: z.string().optional(), addressId: z.string().optional(), notes: z.string().optional(), priority: z.string().optional(), purpose: z.string().optional(), caller: z.string().optional() });
type Person = z.infer<typeof personSchema>;
type Contact = z.infer<typeof contactSchema>;
type Address = z.infer<typeof addressSchema>;
const roles = ['attorney', 'cpa', 'financialAdviser', 'insuranceAgent', 'funeralHome', 'clergy', 'physician', 'veterinarian', 'school', 'employerHr', 'businessPartner', 'propertyService', 'friendFamily', 'other'] as const;

export function PeopleContacts({ database, dek }: { database: ContinuityDatabase | null; dek: CryptoKey }) {
  const { t } = useTranslation('contacts');
  const [people, setPeople] = useState<Person[]>([]);
  const [contacts, setContacts] = useState<Contact[]>([]);
  const [addresses, setAddresses] = useState<Address[]>([]);
  const [person, setPerson] = useState({ firstName: '', lastName: '', preferredName: '', role: 'adult', relationship: '', phone: '', email: '', governmentIdentifier: '', notes: '' });
  const [contact, setContact] = useState({ displayName: '', role: 'attorney', phone: '', email: '', notes: '' });
  const [saved, setSaved] = useState(false);
  const load = () => { if (!database) return; void Promise.all([createEncryptedRepository(database, dek, 'Person', personSchema).list(), createEncryptedRepository(database, dek, 'Contact', contactSchema).list(), createEncryptedRepository(database, dek, 'Address', addressSchema).list()]).then(([nextPeople, nextContacts, nextAddresses]) => { setPeople(nextPeople); setContacts(nextContacts); setAddresses(nextAddresses); }); };
  useEffect(load, [database, dek]);
  const savePerson = async (event: React.FormEvent) => { event.preventDefault(); if (!database || !person.firstName.trim() || !person.lastName.trim()) return; const now = new Date().toISOString(); const record: Person = { id: crypto.randomUUID(), schemaVersion: 1, createdAt: now, updatedAt: now, legalFirstName: person.firstName.trim(), legalLastName: person.lastName.trim(), preferredName: person.preferredName.trim() || undefined, role: person.role as Person['role'], relationship: person.relationship.trim() || undefined, phone: person.phone.trim() || undefined, email: person.email.trim() || undefined, governmentIdentifier: person.governmentIdentifier.trim() || undefined, governmentIdentifierDisplayPolicy: person.governmentIdentifier.trim() ? 'hidden' : undefined, notes: person.notes.trim() || undefined }; await createEncryptedRepository(database, dek, 'Person', personSchema).put(record); setPerson({ firstName: '', lastName: '', preferredName: '', role: 'adult', relationship: '', phone: '', email: '', governmentIdentifier: '', notes: '' }); setSaved(true); load(); };
  const saveContact = async (event: React.FormEvent) => { event.preventDefault(); if (!database || !contact.displayName.trim()) return; const now = new Date().toISOString(); const record: Contact = { id: crypto.randomUUID(), schemaVersion: 1, createdAt: now, updatedAt: now, displayName: contact.displayName.trim(), role: contact.role as Contact['role'], phone: contact.phone.trim() || undefined, email: contact.email.trim() || undefined, notes: contact.notes.trim() || undefined }; await createEncryptedRepository(database, dek, 'Contact', contactSchema).put(record); setContact({ displayName: '', role: 'attorney', phone: '', email: '', notes: '' }); setSaved(true); load(); };
  return <section className="section-page"><p className="eyebrow">{t('title')}</p><h2>{t('title')}</h2><div className="section-card"><h3>{t('addPerson')}</h3><form onSubmit={(event) => void savePerson(event)}><TextField label={t('firstName')} value={person.firstName} onChange={(event) => setPerson({ ...person, firstName: event.target.value })} required /><TextField label={t('lastName')} value={person.lastName} onChange={(event) => setPerson({ ...person, lastName: event.target.value })} required /><TextField label={t('preferredName')} value={person.preferredName} onChange={(event) => setPerson({ ...person, preferredName: event.target.value })} /><SelectField label={t('role')} value={person.role} onChange={(event) => setPerson({ ...person, role: event.target.value })} options={['adult', 'spouse_partner', 'child', 'dependent', 'guardian_candidate', 'other'].map((value) => ({ value, label: value }))} /><TextField label={t('relationship')} value={person.relationship} onChange={(event) => setPerson({ ...person, relationship: event.target.value })} /><TextField label={t('phone')} value={person.phone} onChange={(event) => setPerson({ ...person, phone: event.target.value })} /><TextField label={t('email')} type="email" value={person.email} onChange={(event) => setPerson({ ...person, email: event.target.value })} /><TextField label={t('governmentIdentifier')} value={person.governmentIdentifier} onChange={(event) => setPerson({ ...person, governmentIdentifier: event.target.value })} helpText={t('identifierHelp')} /><TextArea label={t('notes')} value={person.notes} onChange={(event) => setPerson({ ...person, notes: event.target.value })} /><button className="button button-primary">{t('save')}</button></form></div><div className="section-card"><h3>{t('directory')}</h3><form onSubmit={(event) => void saveContact(event)}><TextField label={t('name')} value={contact.displayName} onChange={(event) => setContact({ ...contact, displayName: event.target.value })} required /><SelectField label={t('contactRole')} value={contact.role} onChange={(event) => setContact({ ...contact, role: event.target.value })} options={roles.map((role) => ({ value: role, label: t(`roles.${role}`) }))} /><TextField label={t('phone')} value={contact.phone} onChange={(event) => setContact({ ...contact, phone: event.target.value })} /><TextField label={t('email')} type="email" value={contact.email} onChange={(event) => setContact({ ...contact, email: event.target.value })} /><TextArea label={t('notes')} value={contact.notes} onChange={(event) => setContact({ ...contact, notes: event.target.value })} /><button className="button button-primary">{t('save')}</button></form>{saved && <p role="status">{t('saved')}</p>}<ul>{contacts.length === 0 ? <li>{t('noContacts')}</li> : contacts.map((item) => <li key={item.id}>{item.displayName} — {t(`roles.${item.role}`)} {item.phone ?? ''} {item.email ?? ''}</li>)}</ul></div><div className="section-card"><h3>{t('people')}</h3>{people.length === 0 ? <p>{t('noPeople')}</p> : <ul>{people.map((item) => <li key={item.id}>{item.preferredName || `${item.legalFirstName} ${item.legalLastName}`} — {t('complete')}: {t('completeValue', { count: Number(Boolean(item.phone)) + Number(Boolean(item.email)) })}</li>)}</ul>}</div><div className="section-card"><h3>{t('professionals')}</h3>{contacts.filter((item) => item.role !== 'friendFamily' && item.role !== 'other').length === 0 ? <p>{t('noProfessionals')}</p> : <ul>{contacts.filter((item) => item.role !== 'friendFamily' && item.role !== 'other').map((item) => <li key={item.id}>{item.displayName} — {t(`roles.${item.role}`)}</li>)}</ul>}{addresses.length > 0 && <p>{t('address')}: {addresses.length}</p>}</div></section>;
}
